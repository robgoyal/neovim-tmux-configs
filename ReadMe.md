```bash
git clone https://github.com/robgoyal/neovim-tmux-configs.git /tmp
mv /tmp/neovim-tmux-configs/.tmux.conf ~/
mkdir -p ~/.config
mv /tmp/neovim-tmux-configs/.config/nvim ~/.config/nvim
rm -r /tmp/neovim-tmux-configs
```
